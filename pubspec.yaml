name: unit_tree_data_structure
description: "The tree data structure"
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
